---
interface Metric { label: string; value: string; }
const { items = [] } = Astro.props as { items: Metric[] };
---
<div class="metrics">
  {items.map((item) => <div class="metric"><strong>{item.value}</strong><span>{item.label}</span></div>)}
</div>
